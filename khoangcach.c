#include <stdio.h>
#include <math.h>
int main (){
    int t;
    scanf("%d", &t);
    while (t --)
    {
        double a, b, c, d;
        double x = sqrt((a - c) * (a - c) + (b - d) * (b - d));
        printf("%.4lf", x);
    }
    

}